<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ваучер {{ voucher.uuid|slice(0,8) }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 2cm; }
        h1 { color: #333; }
        .journal { font-weight: bold; }
    </style>
</head>
<body>
    <h1>ВАУЧЕР</h1> 

    <p><span class="journal">Журнал:</span> {{ voucher.template.title }}</p>
    <p>Описание: {{ voucher.template.description }}</p>
    <p>Условия: {{ voucher.terms }}</p>

    <p>Получатель: {{ voucher.fullName }} ({{ voucher.orcid }})</p>

    <p>Срок действия:
        {{ voucher.activeFrom|date('d.m.Y') }} —
        {{ voucher.activeTo|date('d.m.Y') }}
    </p>

    <p>UUID: {{ voucher.uuid }}</p>
</body>
</html>